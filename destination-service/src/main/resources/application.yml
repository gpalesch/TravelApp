spring:
  application:
    name: destinationdb
  datasource:
    url: jdbc:postgresql://db:5432/destinationdb
    username: destinationUser
    password: destinationPassword
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect